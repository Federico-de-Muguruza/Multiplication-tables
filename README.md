#Información:

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es el número elegido para hacer la tabla de multiplicar.
                                                            [número] [requerido]
  -s, --show     Sirve para mostrar la tabla de multiplicar.
                                                     [booleano] [defecto: false]
  -l, --limit    Sirve para ponerle un límite a la tabla de multiplicar.
                                                          [número] [defecto: 10]
```